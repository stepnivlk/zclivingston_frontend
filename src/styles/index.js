const forceNavDown = {'top': '64px'}
const iconStyle = { color: '#fff' }
const linkStyle = { textDecoration: 'none' }
const containerStyle = { paddingTop: 100 }

const appBar = { position: 'fixed' }

const infoColumn = { width: 50 }

const floatingButton = {
  margin: 0,
  top: 'auto',
  right: 20,
  bottom: 20,
  left: 'auto',
  position: 'fixed',
}

export {
  forceNavDown,
  iconStyle,
  linkStyle,
  containerStyle,
  appBar,
  infoColumn,
  floatingButton
}
